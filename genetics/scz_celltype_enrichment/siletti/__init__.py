"""
siletti — Functions for processing the Siletti et al. (2023) whole-brain atlas
and integrating it with the SEA-AD reference via MetaNeighbor.

Submodules:
    specificity  — Compute cell-type specificity from Siletti cluster-level data
    clusters     — Cluster ID mapping, supercluster metadata, classification
    metaneighbor — Cross-dataset cell-type matching via HVG-based AUROC
"""
